katok = [] #빈배열
def add_data(friend):
    katok.append(None)   #빈칸만들기
    kLen = len(katok) #1
    katok[kLen-1] = friend

add_data('다현')
add_data('정연')
add_data('쯔위')
add_data('사나')
add_data('지효')
add_data('모모')
print(katok)

katok.append(None)
katok


