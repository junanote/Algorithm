## 함수 선언부 (클래스 선언)

## 전역 변수부
katok =[]
select = -1   #1. 추가   2. 삽입  3. 삭제   4. 종료

## 메인 코드부
if __name__  == '__main__'   #  메인함수(진입점)
    while(select !=4) :
        select - int(input('1. 추가 2. 삽입 3. 삭제 4. 종료 --->'))